{
  "name": "brain",
  "build": { "dockerfile": "Dockerfile" },
  "containerEnv": {
    "UV_LINK_MODE": "copy",
    "PYTHONUNBUFFERED": "1"
  },
  "mounts": [
    "source=brain-uv-cache,target=/home/vscode/.cache/uv,type=volume",
    "source=brain-mineru-models,target=/home/vscode/.cache/huggingface,type=volume"
  ],
  "postCreateCommand": "uv sync --frozen || { echo 'WARNING: uv.lock is stale vs pyproject.toml; re-resolving (this rewrites uv.lock — run `uv lock` and commit it deliberately)'; uv sync; }",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "tamasfe.even-better-toml"
      ]
    }
  }
}
