---
title: ""
type: person
aliases: []
topics: []
relations: []
---

# <Name>

## Notes

_(empty)_

## Log

<!-- Dated, source-linked facts appended here, newest LAST.
     One line each: "- YYYY-MM-DD — fact ([[knowledge/<source>]])". -->

_(empty)_

<!--
TEMPLATE — person note. Frontmatter is FIRST so a verbatim copy parses as
a real note; this guidance lives in the body and is ignored by the parser.

Usage: copy to knowledge/people/<slug>.md (slug = lowercase-hyphenated name,
e.g. anna-kowalska), fill in the frontmatter, then delete this comment block.
The node id is the knowledge/-relative path without extension:
people/anna-kowalska.

This directory (knowledge/index/templates/) sits OUTSIDE the enrichment
scan — templates are never embedded, indexed, or surfaced in search.

Relations use a closed vocabulary: works_at, member_of, attended,
stakeholder_in, collaborator_on, met_at, related_to. `target` is a
knowledge/-relative path without extension (e.g. organisations/acme).
`valid_from` / `valid_until` are optional YYYY-MM-DD strings; an absent or
empty `valid_until` means the relation is currently valid. `source` is
optional provenance: the vault-relative no-extension path of the note the
relation was learned from. Example of the full shape:

relations:
  - rel: works_at
    target: organisations/acme
    valid_from: "2025-03-01"
    valid_until: ""
    source: knowledge/meetings/2026/2026-06-12-kern-call
-->
